# How to run Java backend Server

1. Install java and maven in system

2. create a snapshot file typing this code
    `mvn clean install -DskipTests`

3. then call the file by 
    `java -jar target/project-0-0-1-SNAPSHOT.jar`
